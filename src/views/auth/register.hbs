<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ImportadoraON - Registro</title>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="flex flex-col min-h-screen">
    <!-- Encabezado -->
    <header class="header-gradient shadow-lg">
        <div class="container mx-auto px-6 py-4">
            <div class="flex items-center justify-between">
                <div class="flex items-center">
                    <a href="/" class="flex items-center">
                        <div class="bg-white p-2 rounded-full mr-3 shadow-md">
                            <i class="fas fa-box-open text-2xl text-red-600"></i>
                        </div>
                        <span class="text-white text-2xl font-bold">Importadora<span class="text-yellow-300">ON</span></span>
                    </a>
                </div>
            </div>
        </div>
    </header>

    <!-- Contenido Principal -->
    <main class="flex-grow flex items-center justify-center py-12 px-4 sm:px-6 lg:px-8">
        <div class="block mx-auto my-12 p-8 bg-white w-full md:w-1/3 rounded-xl shadow-2xl border border-gray-100">
            <div class="flex flex-col items-center mb-6">
                <div class="bg-red-600 p-4 rounded-full mb-4 shadow-lg floating">
                    <i class="fas fa-user-plus text-3xl text-white"></i>
                </div>
                <h1 class="text-3xl font-bold text-center text-gray-800">CREAR CUENTA</h1>
                <p class="text-gray-600 mt-2">Regístrate para acceder a tu cuenta</p>
            </div>

            <form action="/register" method="POST" class="mt-4">
                <!-- Campo Nombre -->
                <div class="mb-4">
                    <label class="block text-gray-700 mb-2 font-medium">Nombre Completo</label>
                    <div class="relative">
                        <div class="absolute inset-y-0 left-0 pl-3 flex items-center pointer-events-none">
                            <i class="fas fa-user text-red-500"></i>
                        </div>
                        <input 
                            type="text" name="nombre" placeholder="Tu nombre completo"
                            class="pl-10 w-full p-3 bg-white border border-gray-200 rounded-lg text-gray-800 focus:outline-none focus:ring-2 focus:ring-red-500 focus:border-transparent placeholder-gray-400 transition-all input-focus"
                            required
                            minlength="2"
                            maxlength="100"
                            pattern="[A-Za-zÁ-ú\s]{2,}"
                            title="Solo letras y espacios (mínimo 2 caracteres)"
                        >
                    </div>
                </div>

                <!-- Campo Apellido -->
                <div class="mb-4">
                    <label class="block text-gray-700 mb-2 font-medium">Apellido</label>
                    <div class="relative">
                        <div class="absolute inset-y-0 left-0 pl-3 flex items-center pointer-events-none">
                            <i class="fas fa-user-tag text-red-500"></i>
                        </div>
                        <input 
                            type="text" name="apellido" placeholder="Tu apellido"
                            class="pl-10 w-full p-3 bg-white border border-gray-200 rounded-lg text-gray-800 focus:outline-none focus:ring-2 focus:ring-red-500 focus:border-transparent placeholder-gray-400 transition-all input-focus"
                            required
                            minlength="2"
                            maxlength="100"
                            pattern="[A-Za-zÁ-ú\s]{2,}"
                            title="Solo letras y espacios (mínimo 2 caracteres)"
                        >
                    </div>
                </div>

                <!-- Campo Celular -->
                <div class="mb-4">
                    <label class="block text-gray-700 mb-2 font-medium">Celular</label>
                    <div class="relative">
                        <div class="absolute inset-y-0 left-0 pl-3 flex items-center pointer-events-none">
                            <i class="fas fa-mobile-alt text-red-500"></i>
                        </div>
                        <input 
                            type="tel" name="celular" placeholder="Tu número de celular"
                            class="pl-10 w-full p-3 bg-white border border-gray-200 rounded-lg text-gray-800 focus:outline-none focus:ring-2 focus:ring-red-500 focus:border-transparent placeholder-gray-400 transition-all input-focus"
                            required
                            pattern="[0-9]{8,15}"
                            title="Ingrese un número de celular válido"
                            maxlength="15"
                        >
                    </div>
                </div>

                <!-- Campo Email -->
                <div class="mb-4">
                    <label class="block text-gray-700 mb-2 font-medium">Correo Electrónico</label>
                    <div class="relative">
                        <div class="absolute inset-y-0 left-0 pl-3 flex items-center pointer-events-none">
                            <i class="fas fa-envelope text-red-500"></i>
                        </div>
                        <input 
                            type="email" name="email" placeholder="tucorreo@ejemplo.com"
                            class="pl-10 w-full p-3 bg-white border border-gray-200 rounded-lg text-gray-800 focus:outline-none focus:ring-2 focus:ring-red-500 focus:border-transparent placeholder-gray-400 transition-all input-focus"
                            required
                            maxlength="100"
                        >
                    </div>
                </div>

                <!-- Campo Contraseña -->
                <div class="mb-6">
                    <label class="block text-gray-700 mb-2 font-medium">Contraseña</label>
                    <div class="relative">
                        <div class="absolute inset-y-0 left-0 pl-3 flex items-center pointer-events-none">
                            <i class="fas fa-lock text-red-500"></i>
                        </div>
                        <input 
                            type="password" name="password" id="password" placeholder="Mínimo 8 caracteres"
                            class="pl-10 w-full p-3 bg-white border border-gray-200 rounded-lg text-gray-800 focus:outline-none focus:ring-2 focus:ring-red-500 focus:border-transparent placeholder-gray-400 transition-all input-focus"
                            required
                            minlength="8"
                            maxlength="100"
                        >
                        <button type="button" class="absolute inset-y-0 right-0 pr-3 flex items-center" onclick="togglePassword('password')">
                            <i class="fas fa-eye text-red-500/50 hover:text-red-500"></i>
                        </button>
                    </div>
                    <p class="text-xs text-gray-500 mt-1">Mínimo 8 caracteres</p>
                </div>

                <!-- Términos y condiciones -->
                <div class="flex items-center mb-6">
                    <input 
                        type="checkbox" 
                        id="terms" 
                        name="terms" 
                        class="w-4 h-4 text-red-600 bg-gray-100 border-gray-300 rounded focus:ring-red-500 focus:ring-2"
                        required
                    >
                    <label for="terms" class="ml-2 text-sm text-gray-600">
                        Acepto los <a href="#" class="text-red-600 hover:text-red-800 font-medium">Términos y Condiciones</a>
